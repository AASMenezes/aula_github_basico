---
title: "Tutorial Interativo: GitHub com R e RStudio"
tutorial:
  id: github-rstudio-intro
  version: 1.0.0
  author: "Alessandra Menezes"
output: learnr::tutorial
runtime: shiny_prerendered
---

```{r setup, include=FALSE}
library(learnr)
```

# Introdução

Bem-vindo ao tutorial interativo! Vamos aprender como usar Git e GitHub com RStudio em projetos de ciência de dados.

---

# O que é Git?

Git é um sistema de controle de versão que permite salvar versões do seu projeto.

```{r git-question, exercise=TRUE}
# Complete o código para configurar o nome do Git
# Dica: use git config --global
# Exemplo: git config --global user.name "Seu Nome"
```

---

# O que é GitHub?

GitHub é uma plataforma online para armazenar repositórios Git.

::: {.callout-tip}
Você pode usar GitHub para compartilhar seus projetos e colaborar com colegas!
:::

---

# Configuração

Para verificar se o Git está instalado, execute:

```{r check-git, exercise=TRUE}
# Use o comando do sistema para verificar o Git
# Exemplo: system("git --version")
```

---

# Exercício Prático

Crie um script `analise.R` com o seguinte código:

```r
summary(iris)
```

Depois, salve, faça commit e push usando a aba **Git** no RStudio.

---

# Quiz

Qual comando envia suas alterações para o GitHub?

```{r push-quiz, quiz=TRUE}
quiz(
  question("Qual comando é usado para enviar mudanças para o repositório remoto?",
    answer("git commit", correct = FALSE),
    answer("git push", correct = TRUE),
    answer("git pull", correct = FALSE),
    allow_retry = TRUE
  )
)
```

---

# Parabéns!

Você completou o tutorial introdutório 🎉  
Continue praticando e explorando os recursos do Git e GitHub com RStudio.

